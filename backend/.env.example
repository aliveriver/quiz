# LLM API 配置
# 将此文件复制为 .env，并填写你的实际值。
# 绝不要把 .env 提交到版本控制。

# OpenAI-compatible API key
LLM_API_KEY=sk-your-api-key-here

# OpenAI-compatible API base URL（可选，用于自定义 endpoint）
# 留空则使用默认的 OpenAI endpoint。
LLM_BASE_URL=https://api.openai.com/v1
